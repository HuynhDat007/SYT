services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: syt-app
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
